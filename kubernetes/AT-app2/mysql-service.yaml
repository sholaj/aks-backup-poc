apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: at-app2
  labels:
    app.kubernetes.io/name: mysql
    app.kubernetes.io/part-of: aks-backup-poc
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mysql
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
